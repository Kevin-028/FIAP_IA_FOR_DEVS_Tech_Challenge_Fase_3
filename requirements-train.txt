# Prefira WSL2 no Windows se for usar Unsloth.
# Nesta máquina o treino cai em transformers+peft (já no venv do app) e o torch
# precisa ser CUDA 13: pip install "torch==2.14.0+cu130" --index-url https://download.pytorch.org/whl/cu130
unsloth
trl>=0.12.0
peft>=0.13.0
transformers>=4.46.0
datasets>=3.0.0
bitsandbytes>=0.44.0
accelerate>=1.0.0
sentencepiece
protobuf
